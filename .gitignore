# Private working docs - not part of the submission
docs/
design/

# Local helper scripts and the Render blueprint - kept on the machine, not in the repo
scripts/
render.yaml

# Env files
.env
.env.local
