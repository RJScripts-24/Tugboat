# Private working docs - not part of the submission.
# `docs/*` rather than `docs/` so the evidence directory below can be re-included:
# git cannot un-ignore a file whose parent directory is ignored outright.
docs/*
design/*

# The committed evidence artifacts are part of the submission (PRD 12)
!docs/evidence/

# Submission-facing docs and assets (tracked)
!docs/ARCHITECTURE.md
!design/logo-banner.png
!design/landing.png

# Env files
.env
.env.local
